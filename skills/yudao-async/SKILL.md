---
name: yudao-async
description: 在芋道 ruoyi-vue-pro 中开发或排查定时任务、异步执行、消息队列、Redis 缓存、分布式锁、重复请求与限流时使用。覆盖执行与重试语义、租户上下文和并发验证。
---

# 任务、消息与并发

涉及芋道项目代码新增方法时，遵守[新增方法注释约定](../../references/method-comments.md)，交付前逐一检查。

先核对当前项目启用的 starter、调度配置、消息中间件及缓存管理器。目录中存在 RabbitMQ、Kafka 或 RocketMQ 支持，不代表运行环境已启用；Redis Channel 和 Stream 也使用不同消费模型。

按问题读取[实施与定位](references/workflow.md)中对应部分。将业务事务、执行触发、失败重试、去重依据和完成状态写成可检查的链路，再修改实现。涉及租户时核对异步上下文、消息拦截器与缓存键是否继续隔离。

`@Idempotent` 的 TTL 重复请求控制、分布式互斥锁、业务状态幂等与消息 ACK 各有边界；按真实需求选择，不将任一注解视为跨系统事务或恰好一次保证。现有重试/补偿机制需要追到注册配置和业务回调，避免同时叠加两套重试。

验证应覆盖本次改变的失败模式，例如任务重入、同消息重投、事务失败后重试、缓存失效或不同租户同业务键。交付调用链、变更文件和已执行证据；自动任务注册、补发消息、清理缓存等操作依据当前任务授权执行。
