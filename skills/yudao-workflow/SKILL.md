---
name: yudao-workflow
description: 在芋道 ruoyi-vue-pro 中开发、排查 BPM/Flowable 流程定义发布、发起权限、审批人、流程实例、通过/拒绝/退回、委派加签与业务单据联动时使用。
---

# 芋道流程审批

先区分模型编辑、定义发布、实例发起、任务处理及业务结果回写；收集 definition key/version、instanceId、taskId、业务单号、操作者与预期状态。根 POM、BPM POM 与 server 依赖决定模块是否启用，基线中 BPM 默认未启用。

读取[BPM 工作路径](references/workflow.md)，沿当前请求对应链路追踪，使用实际权限码、审批人策略和事务边界。设计中的“审批人”要分发起人自选、策略候选人、当前 assignee 与委派/加签参与者。

模型发布和运行中实例是不同生命周期；更改新定义不能假设已有实例同步变化。业务状态回写需核实际监听器/API和事务时机，避免直接改业务表或 Flowable 表绕过现有流程。

以当前任务涉及的发起权限、正确/错误审批人、状态变化及业务回写验证交付。流程部署、迁移、批量处理审批须遵守任务实际授权；维护插件资料本身不授权操作运行中的流程。
