---
name: yudao-integration
description: 开发、配置或排查芋道 ruoyi-vue-pro 的文件存储、短信邮件站内信、社交登录与 OAuth2 接入、Excel 导入导出及 API 日志。用于已有基础能力集成与调用链核查；业务权限和租户策略按当前项目另行核实。
---

# 芋道基础能力集成

涉及芋道项目代码新增方法时，遵守[新增方法注释约定](../../references/method-comments.md)，交付前逐一检查。

先确认当前项目版本、启用模块和调用方，再从[主题流程](references/workflow.md)选择对应章节。源码定位以[来源基线](../../references/source-index.md)为起点，在当前项目重新查找符号；不要把历史行号或目录存在当成运行证据。

优先复用 `infra` 文件服务、`system` 通知与社交服务、框架 Excel/API 日志能力。跨模块调用先找现有 `api` 接口与 DTO，平台配置与客户端实现只在任务涉及该层时加载。

核查完整链路：入口参数 → 模板/账号/存储配置 → Service → 客户端或异步消费者 → 日志/数据记录 → 调用方结果。短信和邮件返回日志 ID 不能证明投递成功；文件元数据存在不能证明对象可访问；社交身份与系统用户绑定需分别核对。

验证按实际风险选择现有 Mock/数据库测试、文件解析或接口检查。执行前读测试体，区分纯 Mock 与连接真实服务的演示。实际发送消息、变更 OAuth 账号绑定、删除存储对象或改生产配置，只在用户明确授权的目标和范围内执行。交付附当前源码位置、实际执行结果以及未验证的外部环节。
