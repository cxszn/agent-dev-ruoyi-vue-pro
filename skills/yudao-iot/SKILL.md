---
name: yudao-iot
description: 在芋道 ruoyi-vue-pro 中开发和排查 IoT 产品物模型、设备认证与上下行消息、MQTT/HTTP/TCP/UDP/Modbus 等协议、场景规则、数据流转和 OTA 时使用。
---

# 芋道 IoT

先确认当前根 POM、IoT 三个子模块及运行进程：`iot-biz` 是业务侧，`iot-core` 提供共享契约，`iot-gateway` 有独立启动入口。基线中 IoT 未默认聚合进主服务，单看后台菜单或目录不能确认网关已运行。

读取[IoT 链路与验收](references/workflow.md)，按设备接入、物模型、消息、规则或 OTA 选择入口。定位问题时带上设备身份、协议、消息方向、requestId 与网关 serverId，保留租户来源。配置与证据中的设备密钥应脱敏。

协议变更要同时检查网关编解码与业务侧消息契约；业务变更要验证真实状态处理。先用受控报文和隔离设备验证，实物控制、规则动作、固件下发需在用户授权的设备范围内执行。

交付说明代码与运行验证分别覆盖哪些层；“消息已入队”只能证明发送阶段，验收还应按任务检查设备响应、属性持久化、规则结果或 OTA 状态。
